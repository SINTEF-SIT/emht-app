<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="?android:attr/listPreferredItemHeight"
    android:padding="6dip">

    <ImageView
        android:id="@+id/assignedAlarmIcon"
        android:layout_width="wrap_content"
        android:layout_height="fill_parent"
        android:layout_alignParentBottom="true"
        android:layout_alignParentTop="true"
        android:layout_marginRight="6dip"
        android:src="@drawable/ic_launcher"/>

    <TextView
        android:id="@+id/assignedAlarmName"
        android:layout_width="fill_parent"
        android:layout_height="30dip"
        android:layout_alignParentRight="true"
        android:layout_alignParentTop="true"
        android:layout_alignWithParentIfMissing="true"
        android:layout_toRightOf="@id/assignedAlarmIcon"
        android:gravity="center_vertical"
        android:textSize="16sp"
        android:text="DISPATCH TIME"/>

    <TextView
        android:id="@+id/assignedAlarmDescription"
        android:layout_width="fill_parent"
        android:layout_height="26dip"
        android:layout_alignParentBottom="true"
        android:layout_alignParentRight="true"
        android:layout_toRightOf="@id/assignedAlarmIcon"
        android:ellipsize="marquee"
        android:singleLine="true"
        android:textSize="12sp"
        android:layout_below="@+id/assignedAlarmName"
        android:text="INCIDENT ADDRESS"/>
</RelativeLayout>